package com.saas.webapp.repository;
import com.saas.webapp.model.firstinsert;
import org.springframework.data.jpa.repository.JpaRepository;

public interface firstinsertRepository extends JpaRepository<firstinsert, Long> {
}