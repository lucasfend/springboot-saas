package com.saas.webapp.repository;
import org.springframework.data.jpa.repository.JpaRepository;
import com.saas.webapp.model.secondinsert;

public interface secondinsertRepository extends JpaRepository<secondinsert, Long> {
}
